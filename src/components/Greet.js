import { Button } from 'react-bootstrap';

const Greet = () => {
  return (
    <div>
      <p>Hello, world!</p>
      <Button variant="success">Success</Button>
    </div>
  );
};

export default Greet;
